# Commenting Guide（注释规范）

## 1. 目标

本规范用于确保注释：
- 解释“为什么这样做”，而不仅是“做了什么”
- 对维护者有帮助，且不与代码脱节
- 在复杂逻辑、边界条件、兼容性处理上提供上下文

---

## 2. 总原则

1. **必要才注释**：优先写清晰代码，注释用于补充背景与意图。  
2. **解释原因**：注释应回答“为什么这样实现/有哪些约束”。  
3. **就近放置**：注释紧贴被解释代码，避免跨屏。  
4. **保持同步**：改代码必须同步改注释。  
5. **统一术语**：术语前后一致（如“爆发线”“端点爆发”）。

---

## 3. 文件/模块头注释

> 用于说明文件职责与边界。  
> **不强制**每个文件写作者/版权；如法务要求再统一加。

推荐模板：

```swift
//
// CursorTrailBar.swift
// 负责：设置窗口 UI、参数同步、轨迹与特效渲染主流程
// 边界：不处理持久化存储的底层细节（由 SettingsStore 负责）
// 说明：该文件较大，按“数据模型 / UI / 渲染 / AppDelegate”分区维护
//
```

---

## 4. 类型（类/结构体/枚举）注释

使用 `///` 说明职责、使用场景、关键约束。

```swift
/// 设置窗口控制器。
/// 职责：构建设置 UI、同步控件状态、向上层发布配置变更。
/// 注意：仅在主线程使用（@MainActor）。
final class SettingsWindowController: NSWindowController { ... }
```

---

## 5. 函数/方法注释

### 5.1 何时必须写
- 对外 API / 可复用公共方法
- 逻辑复杂、带副作用的方法
- 有边界行为、兼容性分支、性能权衡的方法
- `throws` / 异步 / 状态机切换方法

### 5.2 模板（Swift Doc Comment）

```swift
/// 应用一之闪爆发效果。
///
/// - Parameters:
///   - start: 爆发线起点（屏幕坐标系）
///   - end: 爆发线终点（屏幕坐标系）
///   - velocity: 当前鼠标速度（px/s）
///   - timestamp: 事件时间戳
/// - Note: 仅在 `speedBurstEnabled == true` 且满足冷却阈值时生效。
/// - Important: 调用方需保证在主线程执行。
private func emitThunderDashIfNeeded(...) { ... }
```

### 5.3 `throws` 方法补充

```swift
/// 保存设置到本地存储。
/// - Throws: 当序列化失败或写入失败时抛出错误。
func save(...) throws { ... }
```

---

## 6. 行内/代码块注释

只用于以下场景：
- 复杂算法步骤
- 不直观的业务规则
- 临时 workaround（并注明原因与后续计划）

```swift
// 这里故意做二次 scrollToTop：首帧布局后高度才稳定，避免首次进入时落到底部。
DispatchQueue.main.async { ... }
```

---

## 7. 临时注释标记

统一使用以下标签，便于检索：
- `TODO:` 后续要做但不阻塞当前需求
- `FIXME:` 已知问题，需尽快修复
- `HACK:` 临时方案，需替换
- `NOTE:` 关键背景说明

示例：

```swift
// FIXME: 当前使用线性衰减，后续改为两段式（主爆发 + 尾辉）。
```

---

## 8. 禁止事项（Don’ts）

- 不写“翻译代码式”注释（如“给 x 赋值”）。
- 不写过期注释、不准确注释。
- 不在注释里堆砌无效信息（会议记录、情绪化描述）。
- 不将敏感信息写入注释（密钥、账号、内部地址）。

---

## 9. 质量检查清单（PR 自检）

- [ ] 新增复杂逻辑是否解释了“为什么”
- [ ] 公共方法是否包含参数/返回/副作用说明
- [ ] 临时方案是否带 `TODO/FIXME/HACK`
- [ ] 注释是否与当前实现一致
- [ ] 术语是否统一（例如“爆发线/端点爆发”）

---

## 10. 示例：好 vs 不好

### ✅ 好注释

```swift
// 为避免高速移动时特效闪烁，最小采样间隔固定为 1/120s。
let minimumMoveSampleInterval: CFTimeInterval = 1.0 / 120.0
```

### ❌ 不好注释

```swift
// 定义一个最小采样间隔变量
let minimumMoveSampleInterval: CFTimeInterval = 1.0 / 120.0
```
